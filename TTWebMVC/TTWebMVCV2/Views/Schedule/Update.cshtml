@using TTWebMVCV2.Models.PartialViewModels
@model TTWebMVCV2.Models.ViewModels.ScheduleDefViewModel
@{
    var formButtonViewModel = new FormButtonViewModel(updateUrl: Url.Action("Update"), returnUrl: Url.Action("Index"), deleteUrl: Url.Action("Delete", new { @id = Model.Id }));
}
@using (Html.BeginForm("update", "schedule", FormMethod.Post, new { @id = "updateScheduleForm" }))
{
    <fieldset>
        @Html.EditorForModel()
        <partial name="_FormButtonsPartial" model="formButtonViewModel" />
    </fieldset>
}
<partial name="_DeleteModalPartial" model="formButtonViewModel" />